import { AgeWizardPipe } from './age-wizard.pipe';

describe('AgeWizardPipe', () => {
  it('create an instance', () => {
    const pipe = new AgeWizardPipe();
    expect(pipe).toBeTruthy();
  });
});
