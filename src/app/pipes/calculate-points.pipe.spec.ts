import { CalculatePointsPipe } from './calculate-points.pipe';

describe('CalculatePointsPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculatePointsPipe();
    expect(pipe).toBeTruthy();
  });
});
